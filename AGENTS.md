# Codex Instructions

- After updating `README.ipynb`, run `python convert_ipynb_to_md_and_py.py` to regenerate `README.md` and `README.py`.
- Include the conversion output in the PR testing section.
- No additional tests are required.

## General

* Work in the current Git repository.
* Wait for terminal commands to finish before replying.

## Git

* Do not create new branches.
* Commit your changes with Git.
* If pre-commit fails, fix issues and retry.
* Ensure the worktree is clean before finishing.

## Citations

* When referencing files, use `【F:path†Lstart(-Lend)?】`.
* When referencing terminal output, use `【chunk_id†Lstart(-Lend)?】`.
